<div class="gray posttitle">
  <h2><%= "DojoForum" %></h2><br>
    <div class="cate_tag"><%= link_to 'All', root_path %></div>
    <% @categories.each do |category| %>
      <div class="cate_tag"><%= link_to category.name, category_path(category) %></div>
    <% end %>
  </div>
  <div class="col-md-12">
    <table class="table">
 