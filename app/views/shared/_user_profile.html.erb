<div class="gray posttitle">
      <div class="col-md-1"><%= link_to image_tag(@user.avatar, alt: @user.name, width: 200, height: 150), mypost_user_path(@user) %><br><span><%= @user.name %></span></div>
      <%= @user.intro %>
</div>